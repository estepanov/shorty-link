/**
 * Version of each persisted `redirect_event` row. Increment when adding or changing
 * stored columns so queue consumers and aggregators can branch on the shape.
 */
export const REDIRECT_EVENT_SCHEMA_VERSION = 1 as const;
